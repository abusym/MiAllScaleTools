<Window x:Class="MiAllScaleTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiAllScaleTools"
        mc:Ignorable="d"
        Title="秘奥电子秤数据同步工具 - 中非E113A研发" Height="450" Width="760" MinHeight="450" MinWidth="720"
        FontSize="14"
        FontFamily="Microsoft YaHei UI"
        Background="{DynamicResource AppBackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <RoutedCommand x:Key="RunSyncCommand"/>

        <!-- Industrial (flat / low-contrast) theme tokens -->
        <Color x:Key="AppBackgroundColor">#FFF5F6F7</Color>
        <Color x:Key="CardBackgroundColor">#FFFFFFFF</Color>
        <Color x:Key="CardBorderColor">#26000000</Color>
        <Color x:Key="TextPrimaryColor">#FF2B2F33</Color>
        <Color x:Key="TextSecondaryColor">#FF6A7178</Color>
        <Color x:Key="AccentColor">#FF2B6CB0</Color>
        <Color x:Key="AccentHoverColor">#FF2A63A1</Color>
        <Color x:Key="AccentPressedColor">#FF24578E</Color>
        <Color x:Key="NeutralButtonColor">#FFF2F3F5</Color>
        <Color x:Key="NeutralButtonHoverColor">#FFE8EAEE</Color>
        <Color x:Key="NeutralButtonPressedColor">#FFDEE2E8</Color>
        <Color x:Key="HeaderBarColor">#FFEEF1F4</Color>
        <Color x:Key="GridHeaderColor">#FFEEF1F4</Color>
        <Color x:Key="GridLineColor">#12000000</Color>
        <Color x:Key="SuccessRowColor">#EAF4EE</Color>
        <Color x:Key="FailRowColor">#F7ECEC</Color>
        <Color x:Key="RowAltColor">#FFFAFAFB</Color>

        <SolidColorBrush x:Key="AppBackgroundBrush" Color="{StaticResource AppBackgroundColor}"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackgroundColor}"/>
        <SolidColorBrush x:Key="CardBorderBrush" Color="{StaticResource CardBorderColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}"/>
        <SolidColorBrush x:Key="AccentPressedBrush" Color="{StaticResource AccentPressedColor}"/>
        <SolidColorBrush x:Key="NeutralButtonBrush" Color="{StaticResource NeutralButtonColor}"/>
        <SolidColorBrush x:Key="NeutralButtonHoverBrush" Color="{StaticResource NeutralButtonHoverColor}"/>
        <SolidColorBrush x:Key="NeutralButtonPressedBrush" Color="{StaticResource NeutralButtonPressedColor}"/>
        <SolidColorBrush x:Key="HeaderBarBrush" Color="{StaticResource HeaderBarColor}"/>
        <SolidColorBrush x:Key="GridHeaderBrush" Color="{StaticResource GridHeaderColor}"/>
        <SolidColorBrush x:Key="GridLineBrush" Color="{StaticResource GridLineColor}"/>
        <SolidColorBrush x:Key="SuccessRowBrush" Color="{StaticResource SuccessRowColor}"/>
        <SolidColorBrush x:Key="FailRowBrush" Color="{StaticResource FailRowColor}"/>
        <SolidColorBrush x:Key="RowAltBrush" Color="{StaticResource RowAltColor}"/>

        <!-- Shared "card" container -->
        <Style x:Key="CardBorderStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <!-- Buttons -->
        <Style x:Key="BaseButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" BasedOn="{StaticResource BaseButtonStyle}" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="#26000000"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentPressedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" BasedOn="{StaticResource BaseButtonStyle}" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="{StaticResource NeutralButtonBrush}"/>
            <Setter Property="BorderBrush" Value="#26000000"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource NeutralButtonHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource NeutralButtonPressedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- GroupBox template: header bar + card body -->
        <Style TargetType="{x:Type GroupBox}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupBox}">
                        <Border Style="{StaticResource CardBorderStyle}" Padding="0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0"
                                        Background="{StaticResource HeaderBarBrush}"
                                        Padding="10,8"
                                        BorderBrush="{StaticResource CardBorderBrush}"
                                        BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="{StaticResource AccentBrush}"/>
                                        <TextBlock Grid.Column="1"
                                                   Margin="10,0,0,0"
                                                   Text="{TemplateBinding Header}"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"/>
                                    </Grid>
                                </Border>

                                <Border Grid.Row="1" Padding="{TemplateBinding Padding}" Background="Transparent">
                                    <ContentPresenter/>
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid polish -->
        <Style TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource GridLineBrush}"/>
            <Setter Property="RowHeight" Value="38"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource RowAltBrush}"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserSortColumns" Value="False"/>
        </Style>

        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="{StaticResource GridHeaderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="F5" Command="{StaticResource RunSyncCommand}"/>
    </Window.InputBindings>

    <Window.CommandBindings>
        <CommandBinding Command="{StaticResource RunSyncCommand}" Executed="RunSyncCommand_Executed"/>
    </Window.CommandBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="12" Padding="8, 12, 12, 12" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Row="0" Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="6"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="{StaticResource AccentBrush}" Margin="0,2,12,2"/>
                    <StackPanel Grid.Column="1" Orientation="Vertical">
                        <TextBlock FontSize="16" FontWeight="SemiBold" Text="电子秤 → 秘奥商品同步"/>
                        <TextBlock Margin="0,6,0,0" Foreground="{StaticResource TextSecondaryBrush}"
                                   Text="本软件由中非E113A研发，技术支持微信：1035267。"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnSettings"
                            Width="110" Height="60"
                            Margin="0,0,10,0"
                            FontSize="20"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="BtnSettings_Click"
                            Content="设置"/>
                    <Button x:Name="BtnRun"
                            Width="150" Height="60"
                            FontSize="20"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="BtnRun_Click"
                            Content="开始同步 (F5)"/>
                </StackPanel>

                <!-- Running status (merged here; removed the separate GroupBox) -->
                <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="8,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="TbStatus" Grid.Row="0" FontSize="14" FontWeight="SemiBold" Text="就绪"/>
                    <ProgressBar x:Name="Progress" Grid.Row="1" Margin="0,8,0,0" Height="14" Minimum="0" Maximum="100"/>
                    <TextBlock x:Name="TbSummary" Grid.Row="2" Margin="0,8,0,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="12,0,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <GroupBox Grid.Column="0" Grid.Row="0" Header="同步明细" Padding="0">
                <TextBox x:Name="TbLog"
                         Margin="0"
                         Padding="12,10"
                         IsReadOnly="True"
                         VerticalContentAlignment="Top"
                         VerticalAlignment="Stretch"
                         HorizontalAlignment="Stretch"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         FontSize="13"
                         FontFamily="Consolas"
                         Background="{StaticResource CardBackgroundBrush}"
                         BorderBrush="{StaticResource CardBorderBrush}"
                         BorderThickness="1"/>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
